apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: private-networks 
  namespace: {{ namespace }}
spec:
  chain:
    middlewares:
      - name: private-networks
        namespace: kube-system
